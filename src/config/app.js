export const APP_CONFIG = {
    enableEvents: false, // Set to false to disable event selection and always use global flow
    defaultEventId: 'global', // Default event ID to use when events are disabled
};